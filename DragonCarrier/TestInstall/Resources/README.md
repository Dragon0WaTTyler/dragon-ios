Place `dragon_core_snapshot.json` in this folder to bundle a local Dragon Core snapshot with the TestInstall app.

Expected path:
`Dragon/DragonCarrier/TestInstall/Resources/dragon_core_snapshot.json`

In debug remote snapshot mode, the app can also download and cache the published GitHub snapshot.
Bundled snapshots remain the local fallback when the cached remote snapshot is missing or invalid.
